<Window x:Class="MyFirstWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="325" Width="483"
        WindowStartupLocation="CenterScreen"
        Background="#f3f4f6">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Email -->
        <StackPanel Grid.Row="0" Margin="0,10,0,5">
            <TextBlock Text="Email:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="EmailBox"
                     Height="30"
                     Padding="5"/>
            <!-- Placeholder simulation -->
            <TextBlock x:Name="EmailPlaceholder"
                       Text="Enter your email"
                       Foreground="Gray"
                       Margin="8,0,0,0"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False"
                       />
        </StackPanel>

        <!-- Password -->
        <StackPanel Grid.Row="1" Margin="0,10,0,5">
            <TextBlock Text="Password:" FontWeight="Bold" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox"
                         Height="30"
                         Padding="5"/>
        </StackPanel>

        <!-- Login Button -->
        <Button Grid.Row="2"
                Content="Login"
                Height="35"
                Margin="0,20,0,0"
                Background="#0078D7"
                Foreground="White"
                FontWeight="Bold"
                Click="LoginButton_Click"/>
    </Grid>
</Window>